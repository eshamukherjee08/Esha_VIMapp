<table id="candidate_table">
  <tr>
    <th>Important</th>
    <th>ID</th>
    <th>Name</th>
    <th>Experience</th>
    <th>Current State</th>
    <th>Resume</th>
    <th>Delete Candidate</th>
  </tr>
<% @events.each do |event| %>
  <% event.candidates.each do |candidate| %>
    <div id="mark">
      <tr>
        <% if !candidate.starred %>
          <td style="width:5px"><span id= "span_<%= candidate.id %>" onclick = 'mark_candidate(<%= candidate.id %>)'><u>Mark</u></span></td>
        <% else %>
          <td style="width:5px"><span id= "span_<%= candidate.id %>" ><%= image_tag("star.png") %></span></td>
        <% end %>
        <td><%= candidate.id %></td>
        <td><%= candidate.name %></td>
        <td><%= candidate.exp %></td>
        <td><%= candidate.current_state %></td>
        <td style="width:5px"><%= link_to "Download", download_resume_candidate_path(candidate.id) %></td>
        <td style="width:5px"><span id= "delete_<%= candidate.id %>" onclick = 'delete_candidate(<%= candidate.id %>)'><%= image_tag("delete.png") %></span></td>
      </tr>
    </div>
  <% end %>
<% end %>
</table>